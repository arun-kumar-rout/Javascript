// functions: kuch code ko lekh kar koi nam de sakte ho

// functions mainly three kam keliye hota hey
// 1. jab apka code app turant nehin chalana chahte future main chalana chahte ho
// 2. jab apka code ap reuse karna chahte ho
// 3. jab app code chalana chhahte ho har baar with different data

const helobolo = () => {
    console.log("Hello World!")
}
helobolo();

// params and arguments

// arguments = real value jo hum dete hai function chalate waqt
// parameter = variables jisme value store hota arguments wali

const abcd = (a) => {
    console.log(a); //  a is parameter
}
abcd(34); // 34 is arguments